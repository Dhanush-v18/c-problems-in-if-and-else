#include <stdio.h>
int main() {
    int a,b,c;
    scanf("%d %d %d",&a,&b,&c);
    if  (a+b+c==15)
    {
        printf("Equilateral triangle");
    }
    else
    {
    printf("Not Equilateral triangle");
    }
    return 0;
}
